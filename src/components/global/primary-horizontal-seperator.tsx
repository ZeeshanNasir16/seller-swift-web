import React from 'react';

const PrimaryHorizontalSeperator = () => {
  return (
    <div className='relative h-[2px] w-[60%] mx-auto bg-gradient-to-r from-foreground/0 via-primary/90 to-foreground/0 z-20 block' />
  );
};

export default PrimaryHorizontalSeperator;
